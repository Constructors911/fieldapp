# Constructors911 Field App
Mobile PWA for field crews, backed by the JobTread (Pave) API. See docs/CONTRACT.md.
Dev: `cd server && npm i && npm start` + `cd web && npm i && npm run dev`.
Set JT_GRANT_KEY env var on the server to switch from mock to live JobTread.
